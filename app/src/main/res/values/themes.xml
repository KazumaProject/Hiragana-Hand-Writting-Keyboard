<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.HiraganaHandWriteKeyboard" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HiraganaHandWriteKeyboard" parent="Base.Theme.HiraganaHandWriteKeyboard" />

    <style name="Theme.HiraganaHandwriteKeyboard" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Base surfaces -->
        <item name="android:colorBackground">@color/clay_bg</item>
        <item name="colorSurface">@color/clay_surface</item>

        <!-- Text -->
        <item name="android:textColor">@color/clay_text</item>
        <item name="android:textColorPrimary">@color/clay_text</item>
        <item name="android:textColorSecondary">@color/clay_text_muted</item>

        <!-- Accent -->
        <item name="colorPrimary">@color/clay_accent</item>
        <item name="colorSecondary">@color/clay_accent</item>

        <!-- Buttons (default) -->
        <item name="android:buttonStyle">@style/ClayButton</item>
    </style>

    <style name="ClayButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:background">@drawable/clay_button_bg</item>
        <item name="android:textColor">@color/clay_text</item>
        <item name="android:paddingLeft">14dp</item>
        <item name="android:paddingRight">14dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:insetLeft">0dp</item>
        <item name="android:insetRight">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:elevation">0dp</item>
    </style>
</resources>
